<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <title>Störung melden – Kunigundenstraße</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      max-width: 600px;
      margin: 0 auto;
      padding: 16px;
      line-height: 1.4;
    }
    h1 {
      font-size: 1.4rem;
      margin-bottom: 0.5rem;
    }
    .hint {
      font-size: 0.9rem;
      margin-bottom: 1rem;
    }
    label {
      display: block;
      margin-top: 0.75rem;
      font-weight: 600;
      font-size: 0.95rem;
    }
    input, select, textarea {
      width: 100%;
      padding: 8px;
      margin-top: 4px;
      box-sizing: border-box;
      font-size: 0.95rem;
    }
    textarea {
      min-height: 90px;
      resize: vertical;
    }
    button {
      margin-top: 1.2rem;
      padding: 10px 16px;
      font-size: 1rem;
      border: none;
      cursor: pointer;
      border-radius: 4px;
    }
    .submit {
      background: #111827;
      color: white;
    }
    .note {
      font-size: 0.8rem;
      margin-top: 0.5rem;
    }
  </style>
</head>
<body>

  <h1>Störung oder Schaden melden</h1>
  <p class="hint">
    DE: Bitte beschreibe das Problem möglichst genau.<br>
    EN: Please describe the issue as precisely as possible.<br>
    AR: من فضلك صف المشكلة بدقة قدر الإمكان.<br>
    TR: Lütfen sorunu mümkün olduğunca ayrıntılı açıklayın.
  </p>

  <form method="POST" action=https://paul-sl.app.n8n.cloud/webhook/ticket-form>
    <!-- Sprache -->
    <label for="sprache">
      Sprache / Language / اللغة / Dil
    </label>
    <select id="sprache" name="sprache" required>
      <option value="de">Deutsch</option>
      <option value="en">English</option>
      <option value="ar">العربية</option>
      <option value="tr">Türkçe</option>
    </select>

    <!-- Ort -->
    <label for="ort">
      Ort des Schadens / Location / مكان المشكلة / Sorunun yeri
    </label>
    <input
      id="ort"
      name="ort"
      type="text"
      required
      placeholder="z. B. Zimmer 12, Flur 1. OG / e.g. room 12, corridor 1st floor"
    >

    <!-- Kategorie -->
    <label for="kategorie">
      Kategorie / Category / الفئة / Kategori
    </label>
    <select id="kategorie" name="kategorie" required>
      <option value="">Bitte wählen / Please choose</option>
      <option value="Heizung">Heizung / Heating</option>
      <option value="Sanitär">Sanitär (Bad, WC) / Bathroom, toilet</option>
      <option value="Strom">Strom / Electricity</option>
      <option value="Sicherheit">Sicherheit (Tür, Fenster) / Security (door, window)</option>
      <option value="Sonstiges">Sonstiges / Other</option>
    </select>

    <!-- Beschreibung -->
    <label for="beschreibung">
      Beschreibung des Problems / Description / وصف المشكلة / Sorunun açıklaması
    </label>
    <textarea
      id="beschreibung"
      name="beschreibung"
      required
      placeholder="z. B. Heizung funktioniert nicht seit gestern Abend..."
    ></textarea>

    <!-- Name -->
    <label for="name">
      Name (optional) / الاسم (اختياري) / İsim (isteğe bağlı)
    </label>
    <input
      id="name"
      name="name"
      type="text"
      placeholder="Wenn du möchtest, gib deinen Namen an."
    >

    <!-- Kontakt -->
    <label for="kontakt">
      Kontakt (Telefon oder E-Mail, optional) / İletişim / معلومات التواصل
    </label>
    <input
      id="kontakt"
      name="kontakt"
      type="text"
      placeholder="z. B. 0151…, E-Mail…"
    >

    <button type="submit" class="submit">
      Meldung senden / Submit report / إرسال البلاغ / Bildirimi gönder
    </button>

    <p class="note">
      Nach dem Absenden wird deine Meldung an die Verwaltung übermittelt.
    </p>
  </form>

</body>
</html>
